<Project>
  <PropertyGroup>
    <!-- Prevent Roslyn build host assets (BuildHost-net472/netcore) from being copied into bin/ -->
    <UseSharedCompilation>false</UseSharedCompilation>
  </PropertyGroup>
</Project>
