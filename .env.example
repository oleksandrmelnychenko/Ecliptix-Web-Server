# Ecliptix Environment Variables

# Database Connection String for Akka Persistence
# Required: Connection string for SQL Server database
# Example: Server=localhost;Database=EcliptixMemberships;User Id=your_user;Password=your_password;Encrypt=True;TrustServerCertificate=True;
ECLIPTIX_DB_CONNECTION_STRING=

# Other environment variables may be added here as needed
# Ensure sensitive values are never committed to source control